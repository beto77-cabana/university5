

using Microsoft.EntityFrameworkCore;

namespace UniversityApiBackend
{
    public class NotasCodFuente
    {
            // add-migrations "Create Users table"   //crea la tabla users con columnas y algunas restricciones como la primary key , q es el id del usuario

            //update-Database

            //    script-Migration   //para que salga la sql que se va a ejecutar , no es needs


            // a medida que agregamos mas columnas podemos visualizar el modelo, y tbn verlo por sql express }  
            //siempre que modifiquemos hay que hacer la migraccion y actualizar la bd
            //tbn se puede hacer un Remove-migracion

    }
}
